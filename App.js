import { StatusBar } from 'expo-status-bar';
import { StyleSheet, Text, View } from 'react-native';
import AppNavigation from './navigation/appNavitagion';

export default function App() {
  return (
    <AppNavigation />
  );
}

